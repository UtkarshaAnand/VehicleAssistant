<html>
  head
    block styles
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    link(rel="stylesheet" type="text/css" href="/css/PageStyle.css")
    title Profile
  <body>
    div.top
      <ul>
        <li><a href="/home">Home</a></li>
        li: a(href=`/chatRoom/${user.name}`) Chat Room
        li: a(href="/sellCar/carDetails") Sell a car
        li: a(href="/drive") Drive
        li: a(href="/myCars") My Cars
        li: a(class= "active" href=`/myProfile/${user.name}` style="color: black;") #{user.name}
        li(style="float:right;"): a(href='/logout') Logout
      </ul>

    div.main
      <div class="card" style="width: 38rem; position: absolute; top: 50%;left: 50%; transform: translate(-50%, -50%);">
        img.card-img-top(src=`/${user.profilePicture}` alt= "Profile Picture" style="width: 40%; height: 15vw; border-radius: 50%;")
        <div class="card-body">
            h5.card-title= user.name
        </div>
        <ul class="list-group list-group-flush">
            li.list-group-item= user.email
            li.list-group-item= user.contactNumber
        </ul>
        <div class="card-body">
          a(href=`/editProfile/${user.name}` class="btn btn-primary" style="margin-right: 20px") Edit
          a(href=`/changePassword/${user.name}` class="btn btn-dark" style="margin-right: 20px") Change Password
          a(href=`/deleteUser/${user.name}` class="btn btn-danger") Delete Account
        </div>
      </div>  

    div.footer
      p &copy; 2020 Vehicle Assistant
  </body>
</html>