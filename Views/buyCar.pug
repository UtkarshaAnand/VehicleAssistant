<html>
  head
    block styles
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    link(rel="stylesheet" type="text/css" href="/css/PageStyle.css")
    title Buy Cars
  <body>
    div.top
      <ul>
        <li><a class="active" href="/home" style="color: black;">Home</a></li>
        li: a(href=`/chatRoom/${user.name}`) Chat Room
        li: a(href="/sellCar/carDetails") Sell a car
        li: a(href="/drive") Drive
        <li><a href="/myCars">My Cars</a><li>
        li: a(href=`/myProfile/${user.name}`) #{user.name}
        li(style="float:right;"): a(href='/logout') Logout 
      </ul>

    div.main
      <div class="card" style="width: 38rem; position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);">
        img.card-img-top(src=`/${car.image}` alt="Card image cap")
        <div class="card-body">
          <h5 class="card-title">#{car.brand} #{car.model}</h5>
          <p class="card-text">Location: #{car.regCity}, #{car.regState}</p> 
          <p class="card-text">Registration Year: #{car.regYear}</p>
          <p class="card-text">Kilometers: #{car.km}</p>
          <p class="card-text">Owner: #{owner.name}</p>
          <p class="card-text">Room Name: #{car.roomName}</p>
          <p class="card-text">#{car.postedAt}</p>
        </div>
      </div>  

    div.footer
      p &copy; 2020 Vehicle Assistant
  </body>
</html>