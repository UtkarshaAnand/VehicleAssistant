<html>
  head
    block styles
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    link(rel="stylesheet" type="text/css" href="/css/PageStyle.css")
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    title Buy Car
  <body onload="myFunction()">
    div.top
      <ul>
        <li><a href="/home">Home</a></li>
        li: a(href="/evaluateCar") Evaluate Car
        <li><a href="/usedCars">Used Cars</a></li>
        li: a(href="/drive") Drive
        <li><a href="/myCars">My Cars</a><li>
        li: a(href=`/myProfile/${user.name}`) #{user.name}
        li(style="float:right;"): a(href='/logout') Logout 
      </ul>

    div.main
      <div class="car-card">
        <div class="car-photo">
          img(src=`/${car.image}` alt="Card image cap") 
        </div>
        <div class="description">
          <h4 style="margin-bottom: 10px;">#{car.brand} #{car.model}</h4>
          p.title(style="font-size: 15px; border-bottom: none; margin-bottom: 0px") #{car.regCity}, #{car.regState} | #{car.regYear} 
          p.title(style="font-size: 15px; border-bottom: none; margin-bottom: 8px") Kilometers: #{car.km}
          p.title(style="font-size: 28px;") <i class="fa fa-inr"></i> #{car.price}L 
          <p style="color: grey"><i class="fa fa-user" aria-hidden="true" style="color: black"></i> #{owner.name}</p>
          <p style="color: grey"><i class="fa fa-pencil" aria-hidden="true" style="color: black"></i> #{car.postedAt}</p>
          <p style="color: grey"><i class="fa fa-calendar-check-o" aria-hidden="true" style="color: black"></i> #{car.auctionDate}, #{car.auctionTime}</p>
          a(href=`/chatRoom/${user.name}/${car._id}` id="timed-btn" class="btn btn-primary disabled" role="button" aria-disabled="true" style="margin-right: 20px") Join Room
        </div>
      </div> 

    div.footer
      p &copy; 2020 Vehicle Assistant
        <a href="https://github.com/UtkarshaAnand/VehicleAssistant" target="_blank" style="font-size: medium;"><i class="fa fa-github" style=" margin-left: 10px;"></a></i>
    
    script.
      function myFunction() {
        var today = new Date().toDateString();
        if(today == "#{car.auctionDate}") {
          var mybtn = document.getElementById('timed-btn');
          mybtn.className = "btn btn-primary";
          mybtn.setAttribute('aria-disabled', 'false');
        }
      }
  </body>
</html>